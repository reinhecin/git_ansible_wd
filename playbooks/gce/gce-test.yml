---
- name: Playbook test to create gce instance
  hosts: localhost
  connection: local
  gather_facts: no

  vars:
     service_account_email: prueba1-ansi@ansi-rh-tst1.iam.gserviceaccount.com
     credentials_file: ansi-rh-tst1-4e88c0dd5470.json
     project_id: ansi-rh-tst1
     machine_type: n1-standard-1
     image: centos-7

  tasks:
    - name: Launch instances
      gce:
        instance_names: ansi_wbs1
        machine_type: "{{ machine_type }}"
        image: "{{ image }}"
        service_account_email: "{{ service_account_email }}"
        credentials_file: "{{ credentials_file }}"
        project_id: "{{ project_id }}"

